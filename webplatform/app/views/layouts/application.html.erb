<!DOCTYPE html>
<html>
<head>
  <title>usdl4edu</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner" style="padding-left:30px;">
    <a class="brand" href="#">usdl4edu</a>
    <% if @isIndex %>
    <a href="#myModal" role="button" class="btn pull-right" data-toggle="modal" style="margin-right:60px">Import Vocabulary</a>
    <%end%>
  </div>
</div>
<div class="divbody">
  <% if flash[:notice] %>
    <div class="alert alert-success">
        <%= flash[:notice] %>
    </div>
  <% end %>
  <% if flash[:error] %>
    <div class="alert alert-error">
        <%= flash[:error] %>
    </div>
  <% end %>
	<div class="row-fluid">
		<div class="span5">
        <div class="tabbable tabs-left"> <!-- Only required for left/right tabs -->
          <ul class="nav nav-tabs">
            <% count=1 %>
            <% @organizations.each do |organization| %>
              <% if count==1 %>
                <li class="active"><a href="#tab<%=count%>" data-toggle="tab" class="btn-large" id="b<%=count%>">
                  <%=organization%>&nbsp;&nbsp;<span class="badge badge-success pull-right"><%= @services.select{|a| a.organization==organization}.size%></span>
                </a></li>
              <% else %>
                <li><a href="#tab<%=count%>" data-toggle="tab" class="btn-large" id="b<%=count%>">
                  <%=organization%>&nbsp;&nbsp;<span class="badge badge-success pull-right"><%= @services.select{|a| a.organization==organization}.size%></span>
                </a></li>
              <% end %>
              <% count+=1 %>
            <%end%>
          </ul>
          <% collapse=1 %>
          <div class="tab-content">

            <% count=1 %>
            <% @organizations.each do |organization| %>
              <% if count==1 %>
                <div class="tab-pane active" id="tab<%=count%>">
                  <% @services.each do |service| %>
                    <% if service.organization==organization %>
                      <% if service.isCourse==false %>
                        <div class="btn-group" style="margin-bottom:10px"><%= link_to service.title, "/service/#{service.id}", :class => "btn btn-small btn-service-title", :rel => 'tooltip', :title => 'Press for more info'%>
                          <%if @idsS%>
                            <%if not @idsS.include? service.id %>
                              <%aux="" %>
                              <%if @ids %>
                                <%aux=@ids+"-"%>
                              <%end%>
                              <% aux += service.id.to_s%>
                              <%= link_to({:action=>'view',:ids=>aux, :idAdded=>service.id},:class=>"btn btn-small") do %>
                                <i class="icon-plus"></i>
                              <%end%>
                            <%end%>
                          <%else%>
                            <%= link_to({:action=>'view',:ids=>service.id, :idAdded=>service.id},:class=>"btn btn-small") do %>
                              <i class="icon-plus"></i>
                            <%end%>
                          <%end%>

                        </div>
                      <% else %>
                        <div style="margin-bottom:10px">
                          <div class="btn-group">
                          <%= link_to service.title, "/course/#{service.id}", :class => "btn btn-small btn-course-title", :rel => 'tooltip', :title => 'Press for more info'%>
                          <button type="button" class="btn btn-small" data-toggle="collapse" data-target="#collapse<%=collapse%>" data-toggle="tooltip" title="Click to expand/collapse course units list">
                            <span class="caret"></span>
                          </button>
                          <%if @idsS%>
                            <%if not @idsS.include? service.id %>
                              <%aux="" %>
                              <%if @ids %>
                                <%aux=@ids+"-"%>
                              <%end%>
                              <% aux += service.id.to_s%>
                              <%= link_to({:action=>'view',:ids=>aux, :idAdded=>service.id}, :class=>"btn btn-small") do %>
                                <i class="icon-plus inline"></i>
                              <%end%>
                            <%end%>
                          <%else%>
                            <%= link_to({:action=>'view',:ids=>service.id, :idAdded=>service.id}, :class=>"btn btn-small") do %>
                              <i class="icon-plus inline"></i>
                            <%end%>
                          <%end%>
                          </div>
                        </div>
                        <div id="collapse<%=collapse%>" class="collapse">
                          <%collapse+=1%>
                          <% service.units.each do |u| %>
                            <div style="margin-bottom:10px">
                              <%= link_to u.title, "/unit/#{u.id}", :class => "btn btn-small btn-service-title2", :rel => 'tooltip', :title => 'Press for more info'%>
                            </div>
                          <% end%>
                        </div>
                      <% end %>
                    <% end %>
                  <% end %>

                </div>
              <% else %>
                <div class="tab-pane" id="tab<%=count%>">
                  <% @services.each do |service| %>
                    <% if service.organization==organization %>
                      <% if service.isCourse==false %>
                        <div class="btn-group" style="margin-bottom:10px"><%= link_to service.title, "/service/#{service.id}", :class => "btn btn-small btn-service-title", :rel => 'tooltip', :title => 'Press for more info'%>
                          <%if @idsS%>
                            <%if not @idsS.include? service.id %>
                              <%aux="" %>
                              <%if @ids %>
                                <%aux=@ids+"-"%>
                              <%end%>
                              <% aux += service.id.to_s%>
                              <%= link_to({:action=>'view',:ids=>aux, :idAdded=>service.id},:class=>"btn btn-small") do %>
                                <i class="icon-plus"></i>
                              <%end%>
                            <%end%>
                          <%else%>
                            <%= link_to({:action=>'view',:ids=>service.id, :idAdded=>service.id},:class=>"btn btn-small") do %>
                              <i class="icon-plus"></i>
                            <%end%>
                          <%end%>
                      </div>
                      <% else %>
                        <div style="margin-bottom:10px">
                          <div class="btn-group">
                          <%= link_to service.title, "/course/#{service.id}", :class => "btn btn-small btn-course-title", :rel => 'tooltip', :title => 'Press for more info'%>
                          <button type="button" class="btn btn-small" data-toggle="collapse" data-target="#collapse<%=collapse%>" data-toggle="tooltip" title="Click to expand/collapse course units list">
                          <span class="caret"></span>
                          </button>
                          <%if @idsS%>
                            <%if not @idsS.include? service.id %>
                              <%aux="" %>
                              <%if @ids %>
                                <%aux=@ids+"-"%>
                              <%end%>
                              <% aux += service.id.to_s%>
                              <%= link_to({:action=>'view',:ids=>aux, :idAdded=>service.id}, :class=>"btn btn-small") do %>
                                <i class="icon-plus inline"></i>
                              <%end%>
                            <%end%>
                          <%else%>
                            <%= link_to({:action=>'view',:ids=>service.id, :idAdded=>service.id}, :class=>"btn btn-small") do %>
                              <i class="icon-plus inline"></i>
                            <%end%>
                          <%end%>
                          </div>
                        </div>
                        <div id="collapse<%=collapse%>" class="collapse">
                          <%collapse+=1%>
                          <% service.units.each do |u| %>
                            <div style="margin-bottom:10px">
                              <%= link_to u.title, "/unit/#{u.id}", :class => "btn btn-small btn-service-title2", :rel => 'tooltip', :title => 'Press for more info'%>
                            </div>
                          <% end%>
                        </div>
                      <% end %>
                    <% end %>
                  <% end %>
                </div>
              <% end %>
              <% count+=1 %>
            <%end%>

          </div>
        </div>
    </div>
		<div class="span7">
			<%= yield %>
		</div>
	</div>
</div>


<% if @isIndex %>
<%= form_tag "/service_import", :multipart => true do %>
<div id="myModal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Import Vocabulary</h3>
  </div>
  <div class="modal-body">
    <p><%= file_field_tag "file" %></p>
  </div>
    <div class="modal-footer">
    
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
    <%= submit_tag "Import file", class: "btn btn-primary"%>
  </div>
</div>
<% end %>
<%end%>


</body>
</html>


<script type="text/javascript">
$(function() { 
  $('a[data-toggle="tab"]').on('shown', function (e) {
    //save the latest tab; use cookies if you like 'em better:
    localStorage.setItem('lastTab', $(e.target).attr('id'));
  });
  var lastTab = localStorage.getItem('lastTab');
  if (lastTab) {
    $('#'+lastTab).tab('show')
  }
});
</script>
